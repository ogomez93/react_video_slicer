// import { compose } from 'recompose';
// import { connect } from 'react-redux';

import FullVideoButton from './FullVideoButton';

// import { getVideoUrl } from 'videos/selectors';
// import { playVideo } from 'videos/actions';

// const mapStateToProps = (state, { videoId }) => ({
//   videoUrl: getVideoUrl(state, videoId)
// });

// const mapDispatchToProps = (dispatch, { videoId }) => ({
//   playFullVideo: dispatch(playVideo(videoId))
// });

export default FullVideoButton;
